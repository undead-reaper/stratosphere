import { Button } from "@/components/ui/button";
import { Upload } from "lucide-react";

const UploadButton = () => {
  return (
    <Button>
      <Upload />
      Upload
    </Button>
  );
};

export default UploadButton;
